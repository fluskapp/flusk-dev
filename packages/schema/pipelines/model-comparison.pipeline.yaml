# Pipeline: Compare model usage and suggest downgrades
name: modelComparison
namespace: analytics
description: Analyze model usage patterns and suggest cost-saving downgrades
package: business-logic

input:
  calls:
    type: LLMCallEntity[]
    description: Array of LLM calls to analyze

output:
  type: ModelComparisonResult
  fields:
    models: { type: "ModelUsage[]", description: Per-model usage stats }
    suggestions: { type: "DowngradeSuggestion[]", description: Model downgrade suggestions }
    potentialSavings: { type: number, description: Total potential savings from downgrades }

steps:
  - id: model-stats
    expr: |
      (() => {
        const groups = {};
        for (const c of $input.calls) {
          if (!groups[c.model]) groups[c.model] = [];
          groups[c.model].push(c);
        }
        return Object.entries(groups).map(([model, items]) => ({
          model,
          count: items.length,
          totalCost: items.reduce((s, c) => s + (c.cost ?? 0), 0),
          avgTokens: items.reduce((s, c) => s + (c.tokens?.total ?? 0), 0) / items.length,
          percentage: 0,
        }));
      })()
    output: rawModels

  - id: total-cost
    expr: "$rawModels.reduce((s, m) => s + m.totalCost, 0)"
    output: totalCost

  - id: with-percentage
    expr: |
      $rawModels.map(m => ({
        ...m,
        percentage: $totalCost > 0 ? (m.totalCost / $totalCost) * 100 : 0,
      }))
    output: models

  - id: sorted-models
    expr: "[...$models].sort((a, b) => b.totalCost - a.totalCost)"
    output: sortedModels

  - id: suggest-downgrades
    expr: |
      $sortedModels
        .filter(m => m.model.includes('gpt-4') && m.avgTokens < 2000)
        .map(m => ({
          from: m.model,
          to: 'gpt-4o-mini',
          callCount: m.count,
          currentCost: m.totalCost,
          estimatedCost: m.totalCost * 0.06,
          savings: m.totalCost * 0.94,
        }))
    output: suggestions

  - id: potential-savings
    expr: "$suggestions.reduce((s, sg) => s + sg.savings, 0)"
    output: potentialSavings

  - id: return
    value:
      models: $sortedModels
      suggestions: $suggestions
      potentialSavings: $potentialSavings

tests:
  - name: empty calls
    input:
      calls: []
    expected:
      models: []
      suggestions: []
      potentialSavings: 0
