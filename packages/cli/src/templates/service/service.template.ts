/** @generated —
 * Template for generating service files
 */

import { toPascalCase } from '../../generators/utils.js';

export function generateServiceTemplate(serviceName: string): string {
  const pascalName = toPascalCase(serviceName);

  return `/**
 * @generated by @flusk/cli
 * ${pascalName} Service
 */
import { getLogger } from '@flusk/logger';

const logger = getLogger().child({ module: '${serviceName}-service' });

export interface ${pascalName}ServiceInterface {
  // Add service method signatures here
  execute(input: unknown): Promise<unknown>;
}

export class ${pascalName}Service implements ${pascalName}ServiceInterface {
  constructor() {
    // Initialize dependencies here
  }

  /**
   * Execute ${serviceName} service logic
   */
  async execute(input: unknown): Promise<unknown> {
    logger.info('executing ${serviceName} service');
    // TODO: Implement service logic
    return input;
  }
}

/**
 * Create and configure ${serviceName} service instance
 */
export function create${pascalName}Service(): ${pascalName}Service {
  return new ${pascalName}Service();
}
`;
}

export function generateServiceTestTemplate(serviceName: string): string {
  const pascalName = toPascalCase(serviceName);

  return `/**
 * @generated by @flusk/cli
 * Tests for ${pascalName} Service
 */

import { describe, it, expect, beforeEach } from 'vitest';
import { ${pascalName}Service, create${pascalName}Service } from './${serviceName}.service.js';

describe('${pascalName}Service', () => {
  let service: ${pascalName}Service;

  beforeEach(() => {
    service = create${pascalName}Service();
  });

  describe('execute', () => {
    it('should execute service logic', async () => {
      // Arrange
      const input = { test: 'data' };

      // Act
      const result = await service.execute(input);

      // Assert
      expect(result).toBeDefined();
    });

    it('should handle errors gracefully', async () => {
      // Arrange
      const invalidInput = null;

      // Act & Assert
      await expect(service.execute(invalidInput)).resolves.toBeDefined();
    });
  });
});
`;
}
