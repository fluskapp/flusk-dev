/**
 * @generated by @flusk/cli
 * Template for TUI React hooks
 */

import { toCamelCase } from '../../generators/utils.js';

export function generateTuiHookTemplate(name: string): string {
  const camel = toCamelCase(name);
  return `/**
 * @generated by @flusk/cli — customize after generation
 * TUI Hook: ${camel}
 */

import { useState, useEffect } from 'react';

export interface ${toCamelCase(name).charAt(0).toUpperCase() + toCamelCase(name).slice(1)}Result {
  loading: boolean;
  error: string | null;
}

export function ${camel}() {
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    // TODO: implement hook logic
    setLoading(false);
  }, []);

  return { loading, error };
}
`;
}
