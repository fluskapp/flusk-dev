"use strict";
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * CLI command: flusk guard — scan repo for @generated header violations.
 *
 * WHY: AI agents keep adding fake @generated headers to hand-written
 * files. This command detects violations so CI can block them.
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.guardCommand = void 0;
exports.registerEnhancedGuard = registerEnhancedGuard;
var commander_1 = require("commander");
var chalk_1 = require("chalk");
var node_path_1 = require("node:path");
var node_fs_1 = require("node:fs");
var minimatch_1 = require("minimatch");
function walk(dir) {
    var results = [];
    for (var _i = 0, _a = (0, node_fs_1.readdirSync)(dir, { withFileTypes: true }); _i < _a.length; _i++) {
        var entry = _a[_i];
        var full = (0, node_path_1.resolve)(dir, entry.name);
        if (entry.isDirectory()) {
            if (entry.name === 'node_modules' || entry.name === '.git')
                continue;
            results.push.apply(results, walk(full));
        }
        else if (entry.name.endsWith('.ts') && !entry.name.endsWith('.d.ts')) {
            results.push(full);
        }
    }
    return results;
}
function hasGeneratedHeader(filePath) {
    var head = (0, node_fs_1.readFileSync)(filePath, 'utf-8').slice(0, 500);
    return head.includes('@generated');
}
function matchesAny(rel, patterns) {
    return patterns.some(function (p) { return (0, minimatch_1.minimatch)(rel, p); });
}
exports.guardCommand = new commander_1.Command('guard')
    .description('Scan for @generated header violations')
    .option('--json', 'Output machine-readable JSON')
    .action(function (opts) { return __awaiter(void 0, void 0, void 0, function () {
    var root, cfgPath, config, allFiles, errors, warnings, _i, allFiles_1, abs, rel, hasHeader, isProtected, isGenOwned, _a, errors_1, e, _b, warnings_1, w, total;
    return __generator(this, function (_c) {
        root = process.cwd();
        cfgPath = (0, node_path_1.resolve)(root, '.flusk/protected.json');
        if (!(0, node_fs_1.existsSync)(cfgPath)) {
            console.error(chalk_1.default.red('Missing .flusk/protected.json'));
            process.exit(1);
        }
        config = JSON.parse((0, node_fs_1.readFileSync)(cfgPath, 'utf-8'));
        allFiles = walk((0, node_path_1.resolve)(root, 'packages'));
        errors = [];
        warnings = [];
        for (_i = 0, allFiles_1 = allFiles; _i < allFiles_1.length; _i++) {
            abs = allFiles_1[_i];
            rel = (0, node_path_1.relative)(root, abs);
            hasHeader = hasGeneratedHeader(abs);
            isProtected = matchesAny(rel, config.protectedPaths);
            isGenOwned = matchesAny(rel, config.generatorOwnedPaths);
            if (isProtected && hasHeader) {
                errors.push("FAKE HEADER in protected file: ".concat(rel));
            }
            else if (hasHeader && !isGenOwned && !isProtected) {
                warnings.push("@generated header on non-generator file: ".concat(rel));
            }
            else if (isGenOwned && !hasHeader) {
                warnings.push("Generator-owned file missing @generated: ".concat(rel));
            }
        }
        if (opts.json) {
            console.log(JSON.stringify({ errors: errors, warnings: warnings }));
        }
        else {
            for (_a = 0, errors_1 = errors; _a < errors_1.length; _a++) {
                e = errors_1[_a];
                console.error(chalk_1.default.red("\u274C ERROR: ".concat(e)));
            }
            for (_b = 0, warnings_1 = warnings; _b < warnings_1.length; _b++) {
                w = warnings_1[_b];
                console.warn(chalk_1.default.yellow("\u26A0\uFE0F  WARN: ".concat(w)));
            }
            total = errors.length + warnings.length;
            if (total === 0) {
                console.log(chalk_1.default.green('✅ No guard violations found.'));
            }
            else {
                console.log("\n".concat(errors.length, " error(s), ").concat(warnings.length, " warning(s)"));
            }
        }
        if (errors.length > 0)
            process.exit(1);
        return [2 /*return*/];
    });
}); });
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
var forge_1 = require("@flusk/forge");
/** Enhanced guard checks — runs after the base guard command */
function registerEnhancedGuard(cmd) {
    var _this = this;
    cmd.hook('postAction', function () { return __awaiter(_this, void 0, void 0, function () {
        var root, result, _i, _a, f, _b, _c, t, _d, _e, l;
        return __generator(this, function (_f) {
            root = process.cwd();
            result = (0, forge_1.runEnhancedGuard)(root);
            // Missing headers
            for (_i = 0, _a = result.missingHeaders; _i < _a.length; _i++) {
                f = _a[_i];
                console.warn(chalk_1.default.yellow("\u26A0\uFE0F  NEW FILE without @generated: ".concat(f)));
                console.warn(chalk_1.default.gray("   \u2192 Use: flusk g feature <name>"));
            }
            // Tampered GENERATED sections
            for (_b = 0, _c = result.tamperedFiles; _b < _c.length; _b++) {
                t = _c[_b];
                console.error(chalk_1.default.red("\u274C TAMPERED: ".concat(t.filePath, " [").concat(t.sectionLabel, "]")));
            }
            // Large CUSTOM sections
            for (_d = 0, _e = result.largeCustomSections; _d < _e.length; _d++) {
                l = _e[_d];
                console.warn(chalk_1.default.yellow("\u26A0\uFE0F  LARGE CUSTOM (".concat(l.lineCount, " lines): ").concat(l.filePath, " [").concat(l.sectionLabel, "]")));
                console.warn(chalk_1.default.gray("   \u2192 Consider creating a generator"));
            }
            if (result.tamperedFiles.length > 0) {
                process.exitCode = 1;
            }
            return [2 /*return*/];
        });
    }); });
}
// --- END CUSTOM ---
