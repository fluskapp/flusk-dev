/**
 * @generated by @flusk/cli
 */
import { test, describe } from 'node:test';
import assert from 'node:assert';
import { alertsMuteCommand } from './alerts-mute.js';

describe('alerts-mute command', () => {
  test('exports a Command instance', () => {
    assert.strictEqual(alertsMuteCommand.name(), 'alerts-mute');
  });
});
