"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapCommand = void 0;
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * CLI command: flusk map — Open interactive architecture visualization
 */
var commander_1 = require("commander");
var chalk_1 = require("chalk");
var node_path_1 = require("node:path");
var node_fs_1 = require("node:fs");
var node_child_process_1 = require("node:child_process");
exports.mapCommand = new commander_1.Command('map')
    .description('Open interactive architecture map in the browser')
    .option('--no-open', 'Print path instead of opening browser')
    .action(function (opts) {
    var root = process.cwd();
    var htmlPath = (0, node_path_1.resolve)(root, 'docs', 'architecture-map.html');
    if (!(0, node_fs_1.existsSync)(htmlPath)) {
        console.error(chalk_1.default.red("Architecture map not found at ".concat(htmlPath)));
        console.error(chalk_1.default.dim('Expected: docs/architecture-map.html'));
        process.exit(1);
    }
    var fileUrl = "file://".concat(htmlPath);
    if (!opts.open) {
        console.log(fileUrl);
        return;
    }
    console.log(chalk_1.default.blue('🗺️  Opening Flusk Architecture Map...'));
    console.log(chalk_1.default.dim(htmlPath));
    var cmd = process.platform === 'darwin' ? 'open' :
        process.platform === 'win32' ? 'start' : 'xdg-open';
    (0, node_child_process_1.exec)("".concat(cmd, " \"").concat(fileUrl, "\""), function (err) {
        if (err) {
            console.error(chalk_1.default.yellow('Could not open browser automatically.'));
            console.log(chalk_1.default.dim("Open manually: ".concat(fileUrl)));
        }
    });
});
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
