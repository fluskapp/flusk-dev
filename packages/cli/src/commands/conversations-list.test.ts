/**
 * @generated by @flusk/cli
 */
import { test, describe } from 'node:test';
import assert from 'node:assert';
import { conversationsListCommand } from './conversations-list.js';

describe('conversations-list command', () => {
  test('exports a Command instance', () => {
    assert.strictEqual(conversationsListCommand.name(), 'conversations-list');
  });
});
