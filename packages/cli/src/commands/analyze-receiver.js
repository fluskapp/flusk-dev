"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __asyncValues = (this && this.__asyncValues) || function (o) {
    if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
    var m = o[Symbol.asyncIterator], i;
    return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function () { return this; }, i);
    function verb(n) { i[n] = o[n] && function (v) { return new Promise(function (resolve, reject) { v = o[n](v), settle(resolve, reject, v.done, v.value); }); }; }
    function settle(resolve, reject, d, v) { Promise.resolve(v).then(function(v) { resolve({ value: v, done: d }); }, reject); }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startReceiver = startReceiver;
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * Ephemeral OTLP receiver — local HTTP server for span ingestion
 * Binds to random port, accepts POST /v1/traces, writes to SQLite
 */
var node_http_1 = require("node:http");
var execution_1 = require("@flusk/execution");
var logger_1 = require("@flusk/logger");
var log = (0, logger_1.createLogger)({ name: 'analyze-receiver' });
function startReceiver(storage, sessionId, redact, authToken) {
    var _this = this;
    var callCount = 0;
    var server = (0, node_http_1.createServer)(function (req, res) { return __awaiter(_this, void 0, void 0, function () {
        var provided, chunks, chunk, e_1_1, body;
        var _a, req_1, req_1_1;
        var _b, e_1, _c, _d;
        var _e;
        return __generator(this, function (_f) {
            switch (_f.label) {
                case 0:
                    if (authToken) {
                        provided = (_e = req.headers['authorization']) === null || _e === void 0 ? void 0 : _e.replace('Bearer ', '');
                        if (provided !== authToken) {
                            res.writeHead(401);
                            res.end('Unauthorized');
                            return [2 /*return*/];
                        }
                    }
                    if (!(req.method === 'POST' && req.url === '/v1/traces')) return [3 /*break*/, 13];
                    chunks = [];
                    _f.label = 1;
                case 1:
                    _f.trys.push([1, 6, 7, 12]);
                    _a = true, req_1 = __asyncValues(req);
                    _f.label = 2;
                case 2: return [4 /*yield*/, req_1.next()];
                case 3:
                    if (!(req_1_1 = _f.sent(), _b = req_1_1.done, !_b)) return [3 /*break*/, 5];
                    _d = req_1_1.value;
                    _a = false;
                    chunk = _d;
                    chunks.push(chunk);
                    _f.label = 4;
                case 4:
                    _a = true;
                    return [3 /*break*/, 2];
                case 5: return [3 /*break*/, 12];
                case 6:
                    e_1_1 = _f.sent();
                    e_1 = { error: e_1_1 };
                    return [3 /*break*/, 12];
                case 7:
                    _f.trys.push([7, , 10, 11]);
                    if (!(!_a && !_b && (_c = req_1.return))) return [3 /*break*/, 9];
                    return [4 /*yield*/, _c.call(req_1)];
                case 8:
                    _f.sent();
                    _f.label = 9;
                case 9: return [3 /*break*/, 11];
                case 10:
                    if (e_1) throw e_1.error;
                    return [7 /*endfinally*/];
                case 11: return [7 /*endfinally*/];
                case 12:
                    try {
                        body = JSON.parse(Buffer.concat(chunks).toString());
                        callCount += processTraces(body, storage, sessionId, redact);
                        res.writeHead(200, { 'content-type': 'application/json' });
                        res.end('{}');
                    }
                    catch (err) {
                        log.error({ error: err }, 'Failed to process traces');
                        res.writeHead(400);
                        res.end('Bad Request');
                    }
                    return [3 /*break*/, 14];
                case 13:
                    res.writeHead(404);
                    res.end('Not Found');
                    _f.label = 14;
                case 14: return [2 /*return*/];
            }
        });
    }); });
    return new Promise(function (resolve) {
        server.listen(0, '127.0.0.1', function () {
            var addr = server.address();
            var port = typeof addr === 'object' ? addr.port : 0;
            log.info("OTLP receiver listening on port ".concat(port));
            resolve({
                port: port,
                server: server,
                close: function () { return new Promise(function (r) { return server.close(function () { return r(); }); }); },
                getCallCount: function () { return callCount; },
            });
        });
    });
}
function processTraces(body, storage, sessionId, redact) {
    var count = 0;
    for (var _i = 0, _a = body.resourceSpans; _i < _a.length; _i++) {
        var rs = _a[_i];
        for (var _b = 0, _c = rs.scopeSpans; _b < _c.length; _b++) {
            var ss = _c[_b];
            for (var _d = 0, _e = ss.spans; _d < _e.length; _d++) {
                var span = _e[_d];
                if (!(0, execution_1.isGenAiSpan)(span))
                    continue;
                try {
                    var parsed = (0, execution_1.parseLlmSpan)(span);
                    var callData = (0, execution_1.mapSpanToLlmCall)(parsed);
                    if (redact) {
                        callData.prompt = '[REDACTED]';
                        callData.response = '[REDACTED]';
                    }
                    storage.llmCalls.create(__assign(__assign({}, callData), { sessionId: sessionId }));
                    count++;
                }
                catch (err) {
                    log.error({ error: err }, 'Failed to ingest span');
                }
            }
        }
    }
    return count;
}
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
