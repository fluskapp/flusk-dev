"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.runBudgetCheck = runBudgetCheck;
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * Budget check logic extracted from analyze command.
 */
var chalk_1 = require("chalk");
var business_logic_1 = require("@flusk/business-logic");
var resources_1 = require("@flusk/resources");
function runBudgetCheck(config, storage, calls) {
    var _a, _b;
    if (!config.budget)
        return;
    var now = new Date();
    var dayStart = new Date(now.getFullYear(), now.getMonth(), now.getDate()).toISOString();
    var monthStart = new Date(now.getFullYear(), now.getMonth(), 1).toISOString();
    var budgetStatus = business_logic_1.budget.checkBudget(config.budget, {
        dailyCost: storage.llmCalls.sumCostSince(dayStart),
        monthlyCost: storage.llmCalls.sumCostSince(monthStart),
        totalCalls: calls.length,
        duplicateCalls: storage.llmCalls.countDuplicates(),
    });
    if (budgetStatus.alerts.length === 0)
        return;
    console.log(chalk_1.default.yellow('\n⚠️  Budget Alerts:'));
    for (var _i = 0, _c = budgetStatus.alerts; _i < _c.length; _i++) {
        var alert_1 = _c[_i];
        console.log(chalk_1.default.yellow("  - ".concat(alert_1)));
    }
    if (((_a = config.alerts) === null || _a === void 0 ? void 0 : _a.onBudgetExceeded) === 'block') {
        console.log(chalk_1.default.red.bold('\n🛑 BUDGET EXCEEDED — onBudgetExceeded is set to "block"'));
    }
    if ((_b = config.alerts) === null || _b === void 0 ? void 0 : _b.webhook) {
        new resources_1.WebhookClient(config.alerts.webhook).fireAndForget(budgetStatus.alerts);
    }
}
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
