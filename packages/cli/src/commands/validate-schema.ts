/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * Validate Schema Command - flusk validate:schema
 */

import { Command } from 'commander';
import { validateSchemas } from '@flusk/forge';
import { formatError, formatSuccess, formatWarning, formatErrorWithFix } from '../utils/ui.js';
import ora from 'ora';

export const validateSchemaCommand = new Command('validate:schema')
  .description('Validate TypeBox entity schemas')
  .action(async () => {
    const spinner = ora('Validating entity schemas...').start();

    try {
      const result = await validateSchemas();

      spinner.stop();

      if (result.valid) {
        console.log(formatSuccess('All entity schemas are valid!'));
      } else {
        console.log(formatError('Schema validation failed'));
      }

      // Display errors
      if (result.errors.length > 0) {
        console.log('\n❌ Errors:');
        for (const error of result.errors) {
          if (error.fix) {
            console.log(formatErrorWithFix(
              `${error.file}: ${error.message}`,
              error.fix
            ));
          } else {
            console.log(formatError(`${error.file}: ${error.message}`));
          }
        }
      }

      // Display warnings
      if (result.warnings.length > 0) {
        console.log('\n⚠️  Warnings:');
        for (const warning of result.warnings) {
          console.log(formatWarning(`${warning.file}: ${warning.message}`));
        }
      }

      // Exit with appropriate code
      process.exit(result.valid ? 0 : 1);
    } catch (error) {
      spinner.stop();
      console.error(formatError('Validation failed'));
      console.error(error instanceof Error ? error.message : String(error));
      process.exit(1);
    }
  });
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
