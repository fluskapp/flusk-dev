/**
 * @generated by @flusk/cli
 */
import { test, describe } from 'node:test';
import assert from 'node:assert';
import { conversationsShowCommand } from './conversations-show.js';

describe('conversations-show command', () => {
  test('exports a Command instance', () => {
    assert.strictEqual(conversationsShowCommand.name(), 'conversations-show');
  });
});
