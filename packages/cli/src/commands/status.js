"use strict";
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * CLI command: flusk status — overview of generated file health.
 *
 * WHY: Developers need a quick way to see which generated files
 * are stale, how many custom sections exist, and the overall
 * generation health of their project.
 */
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.statusCommand = void 0;
var commander_1 = require("commander");
var chalk_1 = require("chalk");
var node_fs_1 = require("node:fs");
var forge_1 = require("@flusk/forge");
exports.statusCommand = new commander_1.Command('status')
    .description('Show generation status: stale files, custom sections')
    .action(function () {
    var _a;
    var root = process.cwd();
    var report = (0, forge_1.detectChanges)(root, forge_1.DEFAULT_SCAN_DIRS);
    console.log(chalk_1.default.blue('\n📊 Generation Status\n'));
    console.log("  Total generated files: ".concat(chalk_1.default.bold(String(report.total))));
    console.log("  Fresh:    ".concat(chalk_1.default.green(String(report.fresh.length))));
    console.log("  Stale:    ".concat(chalk_1.default.yellow(String(report.stale.length))));
    console.log("  Orphaned: ".concat(chalk_1.default.red(String(report.orphaned.length))));
    var totalCustom = 0;
    var byEntity = new Map();
    for (var _i = 0, _b = __spreadArray(__spreadArray([], report.fresh, true), report.stale, true); _i < _b.length; _i++) {
        var info = _b[_i];
        var entry = (_a = byEntity.get(info.yamlPath)) !== null && _a !== void 0 ? _a : { files: 0, stale: 0, custom: 0 };
        entry.files++;
        if (info.isStale)
            entry.stale++;
        var content = (0, node_fs_1.readFileSync)(info.filePath, 'utf-8');
        var customs = (0, forge_1.extractCustomSections)(content);
        var nonEmpty = __spreadArray([], customs.values(), true).filter(function (v) { return v.trim(); }).length;
        entry.custom += nonEmpty;
        totalCustom += nonEmpty;
        byEntity.set(info.yamlPath, entry);
    }
    console.log("  Custom sections (non-empty): ".concat(chalk_1.default.cyan(String(totalCustom))));
    if (byEntity.size > 0) {
        console.log(chalk_1.default.blue('\n  Per-entity breakdown:'));
        for (var _c = 0, byEntity_1 = byEntity; _c < byEntity_1.length; _c++) {
            var _d = byEntity_1[_c], yaml = _d[0], info = _d[1];
            var staleTag = info.stale ? chalk_1.default.yellow(" (".concat(info.stale, " stale)")) : '';
            console.log("    ".concat(yaml, ": ").concat(info.files, " files").concat(staleTag, ", ").concat(info.custom, " custom sections"));
        }
    }
    console.log('');
});
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
