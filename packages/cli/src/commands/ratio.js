"use strict";
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * CLI command: flusk ratio — generator coverage reporter.
 *
 * WHY: Track progress toward 90% generated code target.
 * Shows generated vs total files per package, with a gap
 * indicator to motivate closing the coverage gap.
 */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ratioCommand = void 0;
var commander_1 = require("commander");
var chalk_1 = require("chalk");
var forge_1 = require("@flusk/forge");
var TARGET_PERCENT = 90;
exports.ratioCommand = new commander_1.Command('ratio')
    .description('Report generator coverage ratio across packages')
    .option('--json', 'Output machine-readable JSON')
    .action(function (opts) { return __awaiter(void 0, void 0, void 0, function () {
    var root, result, pct, gap, _i, _a, _b, pkg, counts, pkgPct, icon;
    return __generator(this, function (_c) {
        root = process.cwd();
        result = (0, forge_1.computeRatio)(root);
        pct = result.total > 0 ? (result.generated / result.total) * 100 : 0;
        gap = Math.max(0, TARGET_PERCENT - pct);
        if (opts.json) {
            console.log(JSON.stringify(__assign(__assign({}, result), { percent: +pct.toFixed(1), target: TARGET_PERCENT, gap: +gap.toFixed(1) })));
            return [2 /*return*/];
        }
        console.log(chalk_1.default.blue("\n\uD83D\uDCCA Generator Ratio Report\n"));
        console.log("  Generator ratio: ".concat(result.generated, "/").concat(result.total, " files (").concat(pct.toFixed(1), "%)"));
        console.log("  Target: ".concat(TARGET_PERCENT, "% | Current: ").concat(pct.toFixed(1), "% | Gap: ").concat(gap.toFixed(1), "%\n"));
        console.log(chalk_1.default.dim('  By package:'));
        for (_i = 0, _a = Object.entries(result.byPackage); _i < _a.length; _i++) {
            _b = _a[_i], pkg = _b[0], counts = _b[1];
            pkgPct = counts.total > 0 ? ((counts.generated / counts.total) * 100).toFixed(1) : '0.0';
            icon = +pkgPct >= TARGET_PERCENT ? '✅' : '📈';
            console.log("    ".concat(icon, " ").concat(pkg, ": ").concat(counts.generated, "/").concat(counts.total, " (").concat(pkgPct, "%)"));
        }
        console.log('');
        return [2 /*return*/];
    });
}); });
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
