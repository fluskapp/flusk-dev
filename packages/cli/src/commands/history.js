"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.historyCommand = void 0;
/**
 * @generated by @flusk/cli — command recipe
 */
// --- BEGIN GENERATED ---
/**
 * CLI command: flusk history
 * Show past analyze sessions from SQLite
 */
var commander_1 = require("commander");
var chalk_1 = require("chalk");
var resources_1 = require("@flusk/resources");
exports.historyCommand = new commander_1.Command('history')
    .description('Show past analyze sessions')
    .option('-l, --limit <n>', 'Number of sessions to show', '20')
    .action(function (opts) {
    var storage = (0, resources_1.createSqliteStorage)();
    var sessions = storage.analyzeSessions.list(parseInt(opts.limit, 10), 0);
    if (sessions.length === 0) {
        console.log(chalk_1.default.dim('No analyze sessions found. Run `flusk analyze <script>` first.'));
        return;
    }
    var header = [
        pad('ID', 10),
        pad('Script', 25),
        pad('Duration', 10),
        pad('Calls', 8),
        pad('Cost', 10),
        'Date',
    ].join('  ');
    console.log(chalk_1.default.bold(header));
    console.log(chalk_1.default.dim('─'.repeat(80)));
    for (var _i = 0, sessions_1 = sessions; _i < sessions_1.length; _i++) {
        var s = sessions_1[_i];
        var row = [
            pad(s.id.slice(0, 8), 10),
            pad(s.script, 25),
            pad("".concat(Math.round(s.durationMs / 1000), "s"), 10),
            pad(String(s.totalCalls), 8),
            pad("$".concat(s.totalCost.toFixed(2)), 10),
            s.startedAt.slice(0, 16).replace('T', ' '),
        ].join('  ');
        console.log(row);
    }
});
function pad(s, len) {
    return s.length >= len ? s.slice(0, len) : s + ' '.repeat(len - s.length);
}
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
