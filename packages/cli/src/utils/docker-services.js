"use strict";
/**
 * @generated by @flusk/cli — utility recipe
 * Docker service management functions
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startServices = startServices;
exports.stopServices = stopServices;
exports.resetServices = resetServices;
exports.getLogs = getLogs;
exports.getServicesStatus = getServicesStatus;
// --- BEGIN GENERATED ---
var execa_1 = require("execa");
/**
 * Start Docker Compose services
 */
function startServices() {
    return __awaiter(this, arguments, void 0, function (projectRoot) {
        if (projectRoot === void 0) { projectRoot = process.cwd(); }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, (0, execa_1.execa)('docker', ['compose', 'up', '-d'], {
                        cwd: projectRoot,
                        stdio: 'inherit',
                    })];
                case 1:
                    _a.sent();
                    return [2 /*return*/];
            }
        });
    });
}
/**
 * Stop Docker Compose services
 */
function stopServices() {
    return __awaiter(this, arguments, void 0, function (projectRoot) {
        if (projectRoot === void 0) { projectRoot = process.cwd(); }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, (0, execa_1.execa)('docker', ['compose', 'down'], {
                        cwd: projectRoot,
                        stdio: 'inherit',
                    })];
                case 1:
                    _a.sent();
                    return [2 /*return*/];
            }
        });
    });
}
/**
 * Reset Docker Compose services (stop, remove volumes, restart)
 */
function resetServices() {
    return __awaiter(this, arguments, void 0, function (projectRoot) {
        if (projectRoot === void 0) { projectRoot = process.cwd(); }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, (0, execa_1.execa)('docker', ['compose', 'down', '-v'], {
                        cwd: projectRoot,
                        stdio: 'inherit',
                    })];
                case 1:
                    _a.sent();
                    return [2 /*return*/];
            }
        });
    });
}
/**
 * Get logs from Docker Compose services
 */
function getLogs(service_1) {
    return __awaiter(this, arguments, void 0, function (service, projectRoot) {
        var args;
        if (projectRoot === void 0) { projectRoot = process.cwd(); }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    args = ['compose', 'logs', '-f'];
                    if (service) {
                        args.push(service);
                    }
                    return [4 /*yield*/, (0, execa_1.execa)('docker', args, {
                            cwd: projectRoot,
                            stdio: 'inherit',
                        })];
                case 1:
                    _a.sent();
                    return [2 /*return*/];
            }
        });
    });
}
/**
 * Map Docker state to our state enum
 */
function mapState(state) {
    var lowerState = (state || '').toLowerCase();
    if (lowerState.includes('running'))
        return 'running';
    if (lowerState.includes('exited'))
        return 'exited';
    if (lowerState.includes('restarting'))
        return 'restarting';
    if (lowerState.includes('dead'))
        return 'dead';
    return 'unknown';
}
/**
 * Map Docker health to our health enum
 */
function mapHealth(health) {
    var lowerHealth = (health || '').toLowerCase();
    if (lowerHealth.includes('healthy'))
        return 'healthy';
    if (lowerHealth.includes('unhealthy'))
        return 'unhealthy';
    if (lowerHealth.includes('starting'))
        return 'starting';
    return 'none';
}
/**
 * Parse Docker port mappings
 */
function parsePorts(publishers) {
    if (!Array.isArray(publishers))
        return [];
    return publishers.map(function (p) {
        if (p.PublishedPort && p.TargetPort) {
            return "".concat(p.PublishedPort, ":").concat(p.TargetPort);
        }
        return '';
    }).filter(Boolean);
}
/**
 * Get status of all Docker Compose services
 */
function getServicesStatus() {
    return __awaiter(this, arguments, void 0, function (projectRoot) {
        var stdout, lines, services, _i, lines_1, line, data, _error_1;
        if (projectRoot === void 0) { projectRoot = process.cwd(); }
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    _a.trys.push([0, 2, , 3]);
                    return [4 /*yield*/, (0, execa_1.execa)('docker', ['compose', 'ps', '--format', 'json'], {
                            cwd: projectRoot,
                        })];
                case 1:
                    stdout = (_a.sent()).stdout;
                    if (!stdout.trim()) {
                        return [2 /*return*/, []];
                    }
                    lines = stdout.trim().split('\n');
                    services = [];
                    for (_i = 0, lines_1 = lines; _i < lines_1.length; _i++) {
                        line = lines_1[_i];
                        try {
                            data = JSON.parse(line);
                            services.push({
                                name: data.Service || data.Name || 'unknown',
                                state: mapState(data.State),
                                health: mapHealth(data.Health),
                                ports: parsePorts(data.Publishers || []),
                            });
                        }
                        catch (_error) {
                            // Skip invalid JSON lines
                        }
                    }
                    return [2 /*return*/, services];
                case 2:
                    _error_1 = _a.sent();
                    return [2 /*return*/, []];
                case 3: return [2 /*return*/];
            }
        });
    });
}
// --- END GENERATED ---
