/**
 * @generated by @flusk/cli — tui recipe
 */
// --- BEGIN GENERATED ---
/**
 * TUI Screen: Budget — daily/monthly spending vs limits
 */

import React from 'react';
// @ts-ignore ink types use exports field
import { Box, Text } from 'ink';
import { useApi } from '../hooks/use-api.js';

interface BudgetData {
  daily: { spent: number; limit: number };
  monthly: { spent: number; limit: number };
  topAgents: Array<{ name: string; cost: number }>;
}

export interface BudgetScreenProps {
  endpoint: string;
  apiKey: string;
}

const barColor = (pct: number) => pct > 90 ? 'red' : pct > 70 ? 'yellow' : 'green';

const renderBar = (pct: number) => {
  const filled = Math.round(pct / 5);
  return `${'█'.repeat(filled)}${'░'.repeat(20 - filled)}`;
};

export function BudgetScreen({ endpoint, apiKey }: BudgetScreenProps) {
  const budget = useApi<BudgetData>(endpoint, '/api/v1/budget/status', apiKey);

  if (budget.loading) return <Text>Loading budget...</Text>;
  if (budget.error) return <Text color="red">Error: {budget.error}</Text>;
  const data = budget.data;
  if (!data) return <Text dimColor>No budget data</Text>;

  const dailyPct = Math.min(100, Math.round((data.daily.spent / data.daily.limit) * 100));
  const monthlyPct = Math.min(100, Math.round((data.monthly.spent / data.monthly.limit) * 100));

  return (
    <Box flexDirection="column">
      <Text bold color="cyan">Budget Status</Text>
      <Box marginTop={1} flexDirection="column">
        <Text>Daily:   <Text color={barColor(dailyPct)}>{renderBar(dailyPct)}</Text> ${data.daily.spent.toFixed(2)} / ${data.daily.limit.toFixed(2)} ({dailyPct}%)</Text>
        <Text>Monthly: <Text color={barColor(monthlyPct)}>{renderBar(monthlyPct)}</Text> ${data.monthly.spent.toFixed(2)} / ${data.monthly.limit.toFixed(2)} ({monthlyPct}%)</Text>
      </Box>
      {data.topAgents.length > 0 && (
        <Box marginTop={1} flexDirection="column">
          <Text bold>Top Spenders</Text>
          {data.topAgents.map((a, i) => (
            <Text key={a.name}> {i + 1}. {a.name} — ${a.cost.toFixed(2)}</Text>
          ))}
        </Box>
      )}
    </Box>
  );
}
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
