/**
 * @generated by @flusk/cli — tui recipe
 */
// --- BEGIN GENERATED ---
/**
 * TUI Hook: useLiveFeed — WebSocket client for /v1/feed
 */

import { useState, useEffect, useRef } from 'react';
import { WebSocket } from 'undici';

export interface FeedEvent {
  id: string;
  model: string;
  cost: number;
  timestamp: string;
}

export function useLiveFeed(endpoint: string): FeedEvent[] {
  const [events, setEvents] = useState<FeedEvent[]>([]);
  const wsRef = useRef<WebSocket | null>(null);

  useEffect(() => {
    const wsUrl = endpoint.replace(/^http/, 'ws') + '/v1/feed';
    try {
      const ws = new WebSocket(wsUrl);
      wsRef.current = ws;

      ws.addEventListener('message', (event) => {
        const data = JSON.parse(String(event.data)) as FeedEvent;
        setEvents((prev) => [...prev.slice(-49), data]);
      });

      ws.addEventListener('error', () => {
        // Silently handle — feed is optional
      });

      return () => { ws.close(); };
    } catch {
      return () => {};
    }
  }, [endpoint]);

  return events;
}
// --- END GENERATED ---
// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
