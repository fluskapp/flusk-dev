/**
 * @generated by @flusk/cli — customized
 * TUI Component: SuggestionList — optimization suggestions
 */

import React from 'react';
import { Box, Text } from 'ink';

export interface Suggestion {
  id: string;
  type: string;
  impact: string;
  description: string;
}

export interface SuggestionListProps {
  suggestions: Suggestion[];
}

export function SuggestionList({ suggestions }: SuggestionListProps) {
  if (suggestions.length === 0) {
    return <Text dimColor>No suggestions available</Text>;
  }

  return (
    <Box flexDirection="column">
      <Text bold>💡 Suggestions ({suggestions.length})</Text>
      {suggestions.map((s) => (
        <Text key={s.id}>
          <Text color="green">{`  [${s.type}]`}</Text>
          {` ${s.description} `}
          <Text color="yellow">({s.impact})</Text>
        </Text>
      ))}
    </Box>
  );
}
