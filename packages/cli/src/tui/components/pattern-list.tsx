/**
 * @generated by @flusk/cli — customized
 * TUI Component: PatternList — detected patterns with frequency
 */

import React from 'react';
import { Box, Text } from 'ink';

export interface Pattern {
  id: string;
  type: string;
  frequency: number;
  description: string;
}

export interface PatternListProps {
  patterns: Pattern[];
  limit?: number;
}

export function PatternList({ patterns, limit = 10 }: PatternListProps) {
  const items = patterns.slice(0, limit);

  if (items.length === 0) {
    return <Text dimColor>No patterns detected yet</Text>;
  }

  return (
    <Box flexDirection="column">
      <Text bold>🔍 Patterns ({patterns.length} total)</Text>
      {items.map((p) => (
        <Text key={p.id}>
          <Text color="cyan">{`  [${p.type}]`}</Text>
          {` ${p.description} `}
          <Text dimColor>×{p.frequency}</Text>
        </Text>
      ))}
    </Box>
  );
}
