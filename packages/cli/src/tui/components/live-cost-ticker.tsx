/**
 * @generated by @flusk/cli — customize after generation
 * TUI Component: LiveCostTicker
 */

import React from 'react';
// @ts-ignore ink types use exports field
import { Box, Text } from 'ink';

export interface LiveCostTickerProps {
  totalCost: number;
  costPerSecond: number;
}

export function LiveCostTicker({ totalCost, costPerSecond }: LiveCostTickerProps) {
  return (
    <Box flexDirection="column" marginBottom={1}>
      <Text bold>💰 Live Cost</Text>
      <Text>  Total: <Text color="green" bold>${totalCost.toFixed(4)}</Text></Text>
      <Text>  Rate:  <Text color="cyan">${costPerSecond.toFixed(6)}/sec</Text></Text>
    </Box>
  );
}
