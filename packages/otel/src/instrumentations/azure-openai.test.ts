/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { patchAzureOpenai } from './azure-openai.js';

describe('azure-openai instrumentation', () => {
  it('exports patch function', () => {
    expect(typeof patchAzureOpenai).toBe('function');
  });

  it('does not throw when sdk is missing', () => {
    expect(() => patchAzureOpenai()).not.toThrow();
  });
});
