/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { patchCohere } from './cohere.js';

describe('cohere instrumentation', () => {
  it('exports patch function', () => {
    expect(typeof patchCohere).toBe('function');
  });

  it('does not throw when sdk is missing', () => {
    expect(() => patchCohere()).not.toThrow();
  });
});
