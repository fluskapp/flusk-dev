/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { patchOpenai } from './openai.js';

describe('openai instrumentation', () => {
  it('exports patch function', () => {
    expect(typeof patchOpenai).toBe('function');
  });

  it('does not throw when sdk is missing', () => {
    expect(() => patchOpenai()).not.toThrow();
  });
});
