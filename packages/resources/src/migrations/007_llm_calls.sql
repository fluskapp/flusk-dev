-- @generated by @flusk/cli from llm-call.entity.ts
-- DO NOT EDIT - regenerate using: flusk g llm-call.entity.ts

-- Create llm_calls table
CREATE TABLE IF NOT EXISTS llm_calls (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
  -- TODO: Add columns from entity schema
);

-- Create indexes
CREATE INDEX IF NOT EXISTS idx_llm_calls_created_at ON llm_calls(created_at);
CREATE INDEX IF NOT EXISTS idx_llm_calls_updated_at ON llm_calls(updated_at);

-- Create updated_at trigger
CREATE OR REPLACE FUNCTION update_llm_calls_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER llm_calls_updated_at
  BEFORE UPDATE ON llm_calls
  FOR EACH ROW
  EXECUTE FUNCTION update_llm_calls_updated_at();
