/**
 * @generated by @flusk/forge — repo wrapper
 * Re-exports from multi-file repo with entity-prefixed names
 */

// --- BEGIN GENERATED (do not edit) ---
import * as Repo from '../sqlite/repositories/training-dataset/index.js';
import type { DatabaseSync } from 'node:sqlite';
import type { TrainingDatasetEntity } from '@flusk/entities';

type CreateInput = Omit<TrainingDatasetEntity, 'id' | 'createdAt' | 'updatedAt'>;
type UpdateInput = Partial<CreateInput>;

export function createTrainingDataset(db: DatabaseSync, data: CreateInput): TrainingDatasetEntity {
  return Repo.create(db, data);
}

export function findTrainingDatasetById(db: DatabaseSync, id: string): TrainingDatasetEntity | null {
  return Repo.findById(db, id);
}

export function listTrainingDatasets(db: DatabaseSync, limit?: number, offset?: number): TrainingDatasetEntity[] {
  return Repo.list(db, limit, offset);
}

export function updateTrainingDataset(db: DatabaseSync, id: string, data: UpdateInput): TrainingDatasetEntity | null {
  return Repo.update(db, id, data);
}

export function deleteTrainingDataset(db: DatabaseSync, id: string): boolean {
  return Repo.deleteById(db, id);
}

export function findTrainingDatasetsByTimeRange(db: DatabaseSync, from: string, to: string): TrainingDatasetEntity[] {
  return Repo.findByTimeRange(db, from, to);
}

export type { AggregateOptions as TrainingDatasetAggregateOptions, AggregateResult as TrainingDatasetAggregateResult } from '../sqlite/repositories/training-dataset/aggregate.js';
export function aggregateTrainingDatasets(db: DatabaseSync, opts: Repo.AggregateOptions): Repo.AggregateResult[] {
  return Repo.aggregate(db, opts);
}

export function findByName(db: DatabaseSync, name: string): TrainingDatasetEntity | null {
  return Repo.findByName(db, name) as TrainingDatasetEntity | null;
}

export function listByTenant(db: DatabaseSync, tenantId: string): TrainingDatasetEntity[] {
  return Repo.listByTenant(db, tenantId) as TrainingDatasetEntity[];
}

export function listByStatus(db: DatabaseSync, status: string): TrainingDatasetEntity[] {
  return Repo.listByStatus(db, status) as TrainingDatasetEntity[];
}
// --- END GENERATED ---
