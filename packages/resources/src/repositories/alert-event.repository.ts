/**
 * @generated by @flusk/forge — repo wrapper
 * Re-exports from multi-file repo with entity-prefixed names
 */

// --- BEGIN GENERATED (do not edit) ---
import * as Repo from '../sqlite/repositories/alert-event/index.js';
import type { DatabaseSync } from 'node:sqlite';
import type { AlertEventEntity } from '@flusk/entities';

type CreateInput = Omit<AlertEventEntity, 'id' | 'createdAt' | 'updatedAt'>;
type UpdateInput = Partial<CreateInput>;

export function createAlertEvent(db: DatabaseSync, data: CreateInput): AlertEventEntity {
  return Repo.create(db, data);
}

export function findAlertEventById(db: DatabaseSync, id: string): AlertEventEntity | null {
  return Repo.findById(db, id);
}

export function listAlertEvents(db: DatabaseSync, limit?: number, offset?: number): AlertEventEntity[] {
  return Repo.list(db, limit, offset);
}

export function updateAlertEvent(db: DatabaseSync, id: string, data: UpdateInput): AlertEventEntity | null {
  return Repo.update(db, id, data);
}

export function deleteAlertEvent(db: DatabaseSync, id: string): boolean {
  return Repo.deleteById(db, id);
}

export function findAlertEventsByTimeRange(db: DatabaseSync, from: string, to: string): AlertEventEntity[] {
  return Repo.findByTimeRange(db, from, to);
}
// --- END GENERATED ---
