/**
 * @generated by @flusk/forge — repo wrapper
 * Re-exports from multi-file repo with entity-prefixed names
 */

// --- BEGIN GENERATED (do not edit) ---
import * as Repo from '../sqlite/repositories/training-pair/index.js';
import type { DatabaseSync } from 'node:sqlite';
import type { TrainingPairEntity } from '@flusk/entities';

type CreateInput = Omit<TrainingPairEntity, 'id' | 'createdAt' | 'updatedAt'>;
type UpdateInput = Partial<CreateInput>;

export function createTrainingPair(db: DatabaseSync, data: CreateInput): TrainingPairEntity {
  return Repo.create(db, data);
}

export function findTrainingPairById(db: DatabaseSync, id: string): TrainingPairEntity | null {
  return Repo.findById(db, id);
}

export function listTrainingPairs(db: DatabaseSync, limit?: number, offset?: number): TrainingPairEntity[] {
  return Repo.list(db, limit, offset);
}

export function updateTrainingPair(db: DatabaseSync, id: string, data: UpdateInput): TrainingPairEntity | null {
  return Repo.update(db, id, data);
}

export function deleteTrainingPair(db: DatabaseSync, id: string): boolean {
  return Repo.deleteById(db, id);
}

export function findTrainingPairsByTimeRange(db: DatabaseSync, from: string, to: string): TrainingPairEntity[] {
  return Repo.findByTimeRange(db, from, to);
}

export type { AggregateOptions as TrainingPairAggregateOptions, AggregateResult as TrainingPairAggregateResult } from '../sqlite/repositories/training-pair/aggregate.js';
export function aggregateTrainingPairs(db: DatabaseSync, opts: Repo.AggregateOptions): Repo.AggregateResult[] {
  return Repo.aggregate(db, opts);
}

export function findByCluster(db: DatabaseSync, cluster: string): TrainingPairEntity[] {
  return Repo.findByCluster(db, cluster) as TrainingPairEntity[];
}

export function listByModel(db: DatabaseSync, sourceModel: string): TrainingPairEntity[] {
  return Repo.listByModel(db, sourceModel) as TrainingPairEntity[];
}

export function countByTenant(db: DatabaseSync, tenantId: string): unknown {
  return Repo.countByTenant(db, tenantId) as unknown;
}

export function countByModelGrouped(db: DatabaseSync): unknown[] {
  return Repo.countByModelGrouped(db) as unknown[];
}

export function countByClusterGrouped(db: DatabaseSync): unknown[] {
  return Repo.countByClusterGrouped(db) as unknown[];
}

export function findByDatasetCluster(db: DatabaseSync, cluster: string, quality: string): TrainingPairEntity[] {
  return Repo.findByDatasetCluster(db, cluster, quality) as TrainingPairEntity[];
}
// --- END GENERATED ---
