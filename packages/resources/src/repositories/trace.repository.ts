/**
 * @generated by @flusk/cli — client recipe
 */

// --- BEGIN GENERATED ---
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
import type { TraceEntity } from '@flusk/entities';
import type { DatabaseSync } from 'node:sqlite';

export type CreateTraceInput = Omit<TraceEntity, 'id' | 'createdAt' | 'updatedAt'>;
export type UpdateTraceInput = Partial<CreateTraceInput>;

function toISOString(value: unknown): string {
  if (typeof value === 'string') return value;
  if (value && typeof value === 'object' && 'toISOString' in value) {
    return (value as { toISOString(): string }).toISOString();
  }
  return String(value);
}

/** Convert a SQLite row (snake_case) to TraceEntity (camelCase) */
function rowToEntity(row: Record<string, unknown>): TraceEntity {
  return {
    id: row.id as string,
    createdAt: toISOString(row.created_at),
    updatedAt: toISOString(row.updated_at),
    organizationId: row.organization_id as string,
    name: row.name as string,
    totalCost: row.total_cost as number,
    totalTokens: row.total_tokens as number,
    totalLatencyMs: row.total_latency_ms as number,
    callCount: row.call_count as number,
    status: row.status as TraceEntity['status'],
    startedAt: toISOString(row.started_at),
    completedAt: row.completed_at != null ? toISOString(row.completed_at) : undefined,
  };
}

function toSnake(s: string): string { return s.replace(/([a-z])([A-Z])/g, '$1_$2').toLowerCase(); }
function convertValueForDb(_key: string, value: unknown): unknown {
  return value ?? null;
}

export function createTrace(db: DatabaseSync, data: CreateTraceInput): TraceEntity {
  const stmt = db.prepare(`INSERT INTO traces (organization_id, name, total_cost, total_tokens, total_latency_ms, call_count, status, started_at, completed_at) VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9) RETURNING *`);
  const row = stmt.get(data.organizationId, data.name, data.totalCost, data.totalTokens, data.totalLatencyMs, data.callCount, data.status, data.startedAt, data.completedAt ?? null) as Record<string, unknown>;
  return rowToEntity(row);
}

export function findTraceById(db: DatabaseSync, id: string): TraceEntity | null {
  const stmt = db.prepare('SELECT * FROM traces WHERE id = $1');
  const row = stmt.get(id) as Record<string, unknown> | undefined;
  return row ? rowToEntity(row) : null;
}

export function listTraces(db: DatabaseSync, limit = 50, offset = 0): TraceEntity[] {
  const stmt = db.prepare('SELECT * FROM traces ORDER BY created_at DESC LIMIT $1 OFFSET $2');
  return (stmt.all(limit, offset) as Record<string, unknown>[]).map(rowToEntity);
}

export function updateTrace(db: DatabaseSync, id: string, data: UpdateTraceInput): TraceEntity | null {
  const keys = Object.keys(data).filter((k) => data[k as keyof typeof data] !== undefined);
  if (keys.length === 0) return findTraceById(db, id);
  const sets = keys.map((k) => `${toSnake(k)} = ?`).join(', ');
  const vals = keys.map((k) => convertValueForDb(k, data[k as keyof typeof data])) as (string | number | null)[];
  const stmt = db.prepare(`UPDATE traces SET ${sets} WHERE id = ? RETURNING *`);
  const row = stmt.get(...vals, id) as Record<string, unknown> | undefined;
  return row ? rowToEntity(row) : null;
}

export function deleteTrace(db: DatabaseSync, id: string): boolean {
  const stmt = db.prepare('DELETE FROM traces WHERE id = $1');
  return stmt.run(id).changes > 0;
}

// --- BEGIN CUSTOM ---

// --- END CUSTOM ---
// --- END CUSTOM ---
