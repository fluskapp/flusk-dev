/**
 * @generated by @flusk/cli — client recipe
 */

// --- BEGIN GENERATED ---
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
import type { ConversionEntity } from '@flusk/entities';
import type { DatabaseSync } from 'node:sqlite';

export type CreateConversionInput = Omit<ConversionEntity, 'id' | 'createdAt' | 'updatedAt'>;
export type UpdateConversionInput = Partial<CreateConversionInput>;

function toISOString(value: unknown): string {
  if (typeof value === 'string') return value;
  if (value && typeof value === 'object' && 'toISOString' in value) {
    return (value as { toISOString(): string }).toISOString();
  }
  return String(value);
}

/** Convert a SQLite row (snake_case) to ConversionEntity (camelCase) */
function rowToEntity(row: Record<string, unknown>): ConversionEntity {
  return {
    id: row.id as string,
    createdAt: toISOString(row.created_at),
    updatedAt: toISOString(row.updated_at),
    patternId: row.pattern_id as string,
    organizationId: row.organization_id as string,
    conversionType: row.conversion_type as string,
    status: row.status as string,
    estimatedSavings: row.estimated_savings as number,
    config: JSON.parse(row.config as string),
  };
}

function toSnake(s: string): string { return s.replace(/([a-z])([A-Z])/g, '$1_$2').toLowerCase(); }
function convertValueForDb(key: string, value: unknown): unknown {
  if (new Set(['config']).has(key)) return JSON.stringify(value);
  return value ?? null;
}

export function createConversion(db: DatabaseSync, data: CreateConversionInput): ConversionEntity {
  const stmt = db.prepare(`INSERT INTO conversions (pattern_id, organization_id, conversion_type, status, estimated_savings, config) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *`);
  const row = stmt.get(data.patternId, data.organizationId, data.conversionType, data.status, data.estimatedSavings, JSON.stringify(data.config)) as Record<string, unknown>;
  return rowToEntity(row);
}

export function findConversionById(db: DatabaseSync, id: string): ConversionEntity | null {
  const stmt = db.prepare('SELECT * FROM conversions WHERE id = $1');
  const row = stmt.get(id) as Record<string, unknown> | undefined;
  return row ? rowToEntity(row) : null;
}

export function listConversions(db: DatabaseSync, limit = 50, offset = 0): ConversionEntity[] {
  const stmt = db.prepare('SELECT * FROM conversions ORDER BY created_at DESC LIMIT $1 OFFSET $2');
  return (stmt.all(limit, offset) as Record<string, unknown>[]).map(rowToEntity);
}

export function updateConversion(db: DatabaseSync, id: string, data: UpdateConversionInput): ConversionEntity | null {
  const keys = Object.keys(data).filter((k) => data[k as keyof typeof data] !== undefined);
  if (keys.length === 0) return findConversionById(db, id);
  const sets = keys.map((k) => `${toSnake(k)} = ?`).join(', ');
  const vals = keys.map((k) => convertValueForDb(k, data[k as keyof typeof data]));
  const stmt = db.prepare(`UPDATE conversions SET ${sets} WHERE id = ? RETURNING *`);
  const row = stmt.get(...vals, id) as Record<string, unknown> | undefined;
  return row ? rowToEntity(row) : null;
}

export function deleteConversion(db: DatabaseSync, id: string): boolean {
  const stmt = db.prepare('DELETE FROM conversions WHERE id = $1');
  return stmt.run(id).changes > 0;
}

// --- BEGIN CUSTOM ---

// --- END CUSTOM ---
// --- END CUSTOM ---
