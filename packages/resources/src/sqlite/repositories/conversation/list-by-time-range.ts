/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';
import type { ConversationEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * List conversations in a time range
 */
export function listByTimeRange(
  db: DatabaseSync, from: string, to: string,
): ConversationEntity[] {
  const stmt = db.prepare(
    'SELECT * FROM conversations WHERE first_call_at >= ? AND last_call_at <= ? ORDER BY last_call_at DESC',
  );
  const rows = stmt.all(from, to) as Record<string, unknown>[];
  return rows.map(rowToEntity);
}
// --- END GENERATED ---
