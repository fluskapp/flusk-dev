/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';

export interface TopByTokensRow {
  _: string;
}

/**
 * Top conversations by total tokens
 */
export function topByTokens(db: DatabaseSync): TopByTokensRow[] {
  const stmt = db.prepare(
    'SELECT * FROM conversations ORDER BY (total_input_tokens + total_output_tokens) DESC LIMIT 10',
  );
  return stmt.all() as unknown as TopByTokensRow[];
}
// --- END GENERATED ---
