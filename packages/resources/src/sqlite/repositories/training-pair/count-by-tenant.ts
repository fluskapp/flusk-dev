/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';

/**
 * Count training pairs for a specific tenant
 */
export function countByTenant(db: DatabaseSync, tenantId: string): number {
  const stmt = db.prepare('SELECT COUNT(*) as total FROM training_pairs WHERE tenant_id = ?');
  const row = stmt.get(tenantId) as { total: number };
  return row.total;
}
// --- END GENERATED ---
