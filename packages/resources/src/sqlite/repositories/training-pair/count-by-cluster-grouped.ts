/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';

export interface ClusterCount {
  cluster: string;
  count: number;
}

/**
 * Count training pairs grouped by cluster
 */
export function countByClusterGrouped(db: DatabaseSync): ClusterCount[] {
  const stmt = db.prepare(
    'SELECT cluster, COUNT(*) as count FROM training_pairs WHERE cluster IS NOT NULL GROUP BY cluster ORDER BY count DESC',
  );
  return stmt.all() as unknown as ClusterCount[];
}
// --- END GENERATED ---
