/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';
import type { TrainingPairEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * Find pairs matching a cluster filter for dataset export
 */
export function findByDatasetCluster(
  db: DatabaseSync, cluster: string, quality: string,
): TrainingPairEntity[] {
  const stmt = db.prepare(
    'SELECT * FROM training_pairs WHERE cluster = ? AND quality = ? ORDER BY created_at ASC',
  );
  const rows = stmt.all(cluster, quality) as Record<string, unknown>[];
  return rows.map(rowToEntity);
}
// --- END GENERATED ---
