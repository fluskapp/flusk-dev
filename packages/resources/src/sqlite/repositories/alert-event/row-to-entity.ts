/**
 * @generated by @flusk/cli — sqlite-repo recipe
 * DO NOT EDIT between GENERATED markers
 */

// --- BEGIN GENERATED ---
import type { AlertEventEntity } from '@flusk/entities';
import { toISOString } from '../../../shared/map-row.js';

/** Convert SQLite row to AlertEventEntity */
export function rowToEntity(row: Record<string, unknown>): AlertEventEntity {
  return {
    id: row.id as string,
    createdAt: toISOString(row.created_at),
    updatedAt: toISOString(row.updated_at),
    alertType: row.alert_type as AlertEventEntity['alertType'],
    severity: row.severity as AlertEventEntity['severity'],
    title: row.title as string,
    message: row.message as string,
    channelName: (row.channel_name as string) ?? undefined,
    delivered: Boolean(row.delivered),
    acknowledgedAt: (row.acknowledged_at as string) ?? undefined,
    metadata: row.metadata != null ? JSON.parse(row.metadata as string) : undefined,
  };
}
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
