/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { TrainingDatasetEntity, ClusterSummary } from '@flusk/entities';
import { toISOString } from '../../../shared/map-row.js';

/** Convert SQLite row to TrainingDatasetEntity */
export function rowToEntity(row: Record<string, unknown>): TrainingDatasetEntity {
  return {
    id: row.id as string,
    createdAt: toISOString(row.created_at),
    updatedAt: toISOString(row.updated_at),
    tenantId: (row.tenant_id as string) ?? undefined,
    name: row.name as string,
    sourceModel: row.source_model as string,
    targetModel: (row.target_model as string) ?? undefined,
    pairCount: row.pair_count as number,
    status: row.status as string,
    clusterSummary: JSON.parse(row.cluster_summary as string) as ClusterSummary,
    exportFormat: row.export_format as string,
    exportPath: (row.export_path as string) ?? undefined,
  };
}
// --- END GENERATED ---
