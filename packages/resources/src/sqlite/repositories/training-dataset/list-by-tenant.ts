/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';
import type { TrainingDatasetEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * List datasets for a tenant
 */
export function listByTenant(
  db: DatabaseSync, tenantId: string,
): TrainingDatasetEntity[] {
  const stmt = db.prepare(
    'SELECT * FROM training_datasets WHERE tenant_id = ? ORDER BY created_at DESC',
  );
  const rows = stmt.all(tenantId) as Record<string, unknown>[];
  return rows.map(rowToEntity);
}
// --- END GENERATED ---
