/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';
import type { TrainingDatasetEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * Find dataset by name
 */
export function findByName(
  db: DatabaseSync,
  name: string,
): TrainingDatasetEntity | null {
  const stmt = db.prepare(
    'SELECT * FROM training_datasets WHERE name = ?',
  );
  const row = stmt.get(name) as Record<string, unknown> | undefined;
  return row ? rowToEntity(row) : null;
}
// --- END GENERATED ---
