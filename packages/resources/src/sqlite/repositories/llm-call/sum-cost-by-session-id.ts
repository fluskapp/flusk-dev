/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';

/**
 * Sum total cost of LLM calls for a specific session
 */
export function sumCostBySessionId(db: DatabaseSync, sessionId: string): number {
  const stmt = db.prepare(
    'SELECT COALESCE(SUM(cost), 0) as total FROM llm_calls WHERE session_id = ?',
  );
  const row = stmt.get(sessionId) as { total: number };
  return row.total;
}
// --- END GENERATED ---
