/** @generated by @flusk/forge — multi-file repo */
// --- BEGIN GENERATED (do not edit) ---
import type { DatabaseSync } from 'node:sqlite';
import type { LLMCallEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * List LLM calls filtered by conversation ID
 */
export function listByConversationId(
  db: DatabaseSync, conversationId: string,
): LLMCallEntity[] {
  const stmt = db.prepare(
    'SELECT * FROM llm_calls WHERE conversation_id = ? ORDER BY created_at ASC',
  );
  const rows = stmt.all(conversationId) as Record<string, unknown>[];
  return rows.map(rowToEntity);
}
// --- END GENERATED ---
