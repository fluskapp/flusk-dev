/**
 * @generated by @flusk/cli — sqlite-repo recipe
 * DO NOT EDIT between GENERATED markers
 */

// --- BEGIN GENERATED ---
import type { ExplainSessionEntity } from '@flusk/entities';
import { toISOString } from '../../../shared/map-row.js';

/** Convert SQLite row to ExplainSessionEntity */
export function rowToEntity(row: Record<string, unknown>): ExplainSessionEntity {
  return {
    id: row.id as string,
    createdAt: toISOString(row.created_at),
    updatedAt: toISOString(row.updated_at),
    analyzeSessionId: row.analyze_session_id as string,
    llmProvider: row.llm_provider as string,
    llmModel: row.llm_model as string,
    promptTokens: row.prompt_tokens as number,
    completionTokens: row.completion_tokens as number,
    explainCost: row.explain_cost as number,
    insightsCount: row.insights_count as number,
    totalSavings: row.total_savings as number,
  };
}
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
