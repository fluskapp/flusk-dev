/**
 * @generated by @flusk/cli — sqlite-repo recipe
 * DO NOT EDIT between GENERATED markers
 */

// --- BEGIN GENERATED ---
import type { DatabaseSync } from 'node:sqlite';
import type { AlertChannelEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * Find Alert channel by id
 */
export function findById(
  db: DatabaseSync,
  id: string,
): AlertChannelEntity | null {
  const stmt = db.prepare('SELECT * FROM alert_channels WHERE id = ?');
  const row = stmt.get(id) as Record<string, unknown> | undefined;
  return row ? rowToEntity(row) : null;
}
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
