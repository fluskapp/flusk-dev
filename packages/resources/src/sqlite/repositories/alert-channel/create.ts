/**
 * @generated by @flusk/cli — sqlite-repo recipe
 * DO NOT EDIT between GENERATED markers
 */

// --- BEGIN GENERATED ---
import type { DatabaseSync } from 'node:sqlite';
import type { AlertChannelEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * Insert a new Alert channel record into SQLite
 */
export function create(
  db: DatabaseSync,
  data: Omit<AlertChannelEntity, 'id' | 'createdAt' | 'updatedAt'>,
): AlertChannelEntity {
  const stmt = db.prepare(`
    INSERT INTO alert_channels (
      name, channel_type, config, enabled, severity_filter
    ) VALUES (?, ?, ?, ?, ?)
    RETURNING *
  `);

  const row = stmt.get(
    data.name,
    data.channelType,
    JSON.stringify(data.config),
    data.enabled ? 1 : 0,
    data.severityFilter,
  ) as Record<string, unknown>;

  return rowToEntity(row);
}
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
