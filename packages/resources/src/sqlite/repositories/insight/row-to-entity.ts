/**
 * @generated by @flusk/cli — sqlite-repo recipe
 * DO NOT EDIT between GENERATED markers
 */

// --- BEGIN GENERATED ---
import type { InsightEntity } from '@flusk/entities';
import { toISOString } from '../../../shared/map-row.js';

/** Convert SQLite row to InsightEntity */
export function rowToEntity(row: Record<string, unknown>): InsightEntity {
  return {
    id: row.id as string,
    createdAt: toISOString(row.created_at),
    updatedAt: toISOString(row.updated_at),
    sessionId: row.session_id as string,
    category: row.category as InsightEntity['category'],
    severity: row.severity as InsightEntity['severity'],
    title: row.title as string,
    description: row.description as string,
    currentCost: row.current_cost as number,
    projectedCost: row.projected_cost as number,
    savingsPercent: row.savings_percent as number,
    codeSuggestion: row.code_suggestion as string | undefined,
    provider: row.provider as string,
    model: row.model as string,
  };
}
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
