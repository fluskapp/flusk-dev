/**
 * @generated by @flusk/cli — sqlite-repo recipe
 * DO NOT EDIT between GENERATED markers
 */

// --- BEGIN GENERATED ---
import type { DatabaseSync } from 'node:sqlite';
import type { InsightEntity } from '@flusk/entities';
import { rowToEntity } from './row-to-entity.js';

/**
 * Insert a new Insight record into SQLite
 */
export function create(
  db: DatabaseSync,
  data: Omit<InsightEntity, 'id' | 'createdAt' | 'updatedAt'>,
): InsightEntity {
  const stmt = db.prepare(`
    INSERT INTO insights (
      session_id, category, severity, title, description,
      current_cost, projected_cost, savings_percent, code_suggestion,
      provider, model
    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
    RETURNING *
  `);

  const row = stmt.get(
    data.sessionId,
    data.category,
    data.severity,
    data.title,
    data.description,
    data.currentCost,
    data.projectedCost,
    data.savingsPercent,
    data.codeSuggestion ?? null,
    data.provider,
    data.model,
  ) as Record<string, unknown>;

  return rowToEntity(row);
}
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
// --- END CUSTOM ---
