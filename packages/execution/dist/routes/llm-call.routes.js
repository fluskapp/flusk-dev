/**
 * @generated by @flusk/cli from llm-call.entity.ts
 * DO NOT EDIT - regenerate using: flusk g llm-call.entity.ts
 */
import { Type } from '@sinclair/typebox';
import { LLMCallEntitySchema } from '@flusk/entities';
import * as LLMCallRepository from '@flusk/resources/repositories/llm-call';
/**
 * Request schema for creating LLMCall
 */
const CreateLLMCallSchema = Type.Omit(LLMCallEntitySchema, [
    'id',
    'createdAt',
    'updatedAt'
]);
/**
 * Response schema for LLMCall
 */
const LLMCallResponseSchema = LLMCallEntitySchema;
/**
 * Register LLMCall routes
 */
export async function llmcallRoutes(fastify) {
    /**
     * POST /llm-calls
     * Create new llm-call record
     */
    fastify.post('/', {
        schema: {
            body: CreateLLMCallSchema,
            response: {
                201: LLMCallResponseSchema
            },
            tags: ['LLMCall'],
            description: 'Create a new llm-call record'
        }
    }, async (request, reply) => {
        const data = request.body;
        const created = await LLMCallRepository.create(data);
        return reply.code(201).send(created);
    });
    /**
     * GET /llm-calls/:id
     * Retrieve llm-call by UUID
     */
    fastify.get('/:id', {
        schema: {
            params: Type.Object({
                id: Type.String({ format: 'uuid' })
            }),
            response: {
                200: LLMCallResponseSchema,
                404: Type.Object({ error: Type.String() })
            },
            tags: ['LLMCall'],
            description: 'Get llm-call by ID'
        }
    }, async (request, reply) => {
        const { id } = request.params;
        const entity = await LLMCallRepository.findById(id);
        if (!entity) {
            return reply.code(404).send({ error: 'LLMCall not found' });
        }
        return reply.code(200).send(entity);
    });
}
//# sourceMappingURL=llm-call.routes.js.map