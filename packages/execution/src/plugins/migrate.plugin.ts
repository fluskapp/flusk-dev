/**
 * @generated by @flusk/cli — middleware recipe
 * Migration runner plugin — applies SQLite migrations on startup.
 */

// --- BEGIN GENERATED ---
import fp from 'fastify-plugin';
import { runMigrations, getDb } from '@flusk/resources';
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
export const plugin = fp(async (app) => {
  runMigrations(getDb());
  app.log.info('SQLite migrations applied');
}, { name: 'flusk-migrate' });
// --- END CUSTOM ---
