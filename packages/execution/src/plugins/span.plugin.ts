/**
 * @generated by @flusk/cli — middleware recipe
 * DO NOT EDIT - regenerate using: flusk g span.entity.ts
 */

// --- BEGIN GENERATED ---
import type { FastifyInstance } from 'fastify';
import fp from 'fastify-plugin';
import { spanRoutes } from '../routes/span.routes.js';
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
/**
 * Span plugin - encapsulates all span functionality
 */
async function spanPlugin(
  fastify: FastifyInstance
): Promise<void> {
  // Register routes under /spans prefix
  await fastify.register(spanRoutes, { prefix: '/spans' });

  fastify.log.info('Span plugin registered');
}

// eslint-disable-next-line no-restricted-syntax -- fastify-plugin requires default export
export default fp(spanPlugin, {
  name: 'span-plugin'
});
// --- END CUSTOM ---
