/**
 * @generated by @flusk/cli — middleware recipe
 */

// --- BEGIN GENERATED ---
import type { FastifyInstance, FastifyRequest, FastifyReply } from 'fastify';
import { Type } from '@sinclair/typebox';
import { PatternRepository } from '@flusk/resources';
import { PatternResponseSchema } from './schemas.js';
// --- END GENERATED ---

// --- BEGIN CUSTOM ---
/**
 * GET /patterns/:id
 * Retrieve pattern by UUID
 */
export function registerGetPattern(fastify: FastifyInstance): void {
  fastify.get(
    '/:id',
    {
      schema: {
        params: Type.Object({
          id: Type.String({ format: 'uuid' })
        }),
        response: {
          200: PatternResponseSchema,
          404: Type.Object({
            error: Type.String()
          })
        },
        tags: ['Patterns'],
        description: 'Get pattern by ID'
      }
    },
    async (request: FastifyRequest<{ Params: { id: string } }>, reply: FastifyReply) => {
      const { id } = request.params;

      const pattern = await PatternRepository.findById(id);

      if (!pattern) {
        return reply.code(404).send({ error: 'Pattern not found' });
      }

      return reply.code(200).send(pattern);
    }
  );
}
// --- END CUSTOM ---
