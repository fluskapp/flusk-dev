/**
 * @generated by @flusk/cli from pattern.entity.ts
 * Business logic stub - implement your domain logic here
 */

import { PatternEntity } from '@flusk/types';

/**
 * Example validation function for Pattern
 * Pure function - no side effects, no I/O
 */
export function validatePattern(
  _entity: Partial<PatternEntity>
): { valid: boolean; errors: string[] } {
  const errors: string[] = [];

  // TODO: Add validation logic
  // Example:
  // if (!_entity.someField) {
  //   errors.push('someField is required');
  // }

  return {
    valid: errors.length === 0,
    errors
  };
}
