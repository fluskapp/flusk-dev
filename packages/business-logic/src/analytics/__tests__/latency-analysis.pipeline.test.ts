/**
 * @generated by @flusk/forge — pipeline generator (test)
 */
import { describe, it, expect } from 'vitest';
import { latencyAnalysis } from '../latency-analysis.pipeline.js';
import type { LatencyAnalysisInput } from '../latency-analysis.pipeline.js';

describe('latencyAnalysis pipeline', () => {
  it('empty calls', () => {
    const result = latencyAnalysis({"calls":[]} as unknown as LatencyAnalysisInput);
    expect(result).toEqual({"avgLatencyMs":0,"p95LatencyMs":0,"slowestCalls":[],"byModel":{}});
  });
});
