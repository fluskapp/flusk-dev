/**
 * @generated by @flusk/forge — pipeline generator (test)
 */
import { describe, it, expect } from 'vitest';
import { modelComparison } from '../model-comparison.pipeline.js';
import type { ModelComparisonInput } from '../model-comparison.pipeline.js';

describe('modelComparison pipeline', () => {
  it('empty calls', () => {
    const result = modelComparison({"calls":[]} as unknown as ModelComparisonInput);
    expect(result).toEqual({"models":[],"suggestions":[],"potentialSavings":0});
  });
});
