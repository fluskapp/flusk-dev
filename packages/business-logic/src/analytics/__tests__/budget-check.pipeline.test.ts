/**
 * @generated by @flusk/forge — pipeline generator (test)
 */
import { describe, it, expect } from 'vitest';
import { budgetCheck } from '../budget-check.pipeline.js';
import type { BudgetCheckInput } from '../budget-check.pipeline.js';

describe('budgetCheck pipeline', () => {
  it('empty calls under budget', () => {
    const result = budgetCheck({"calls":[],"dailyLimit":10,"monthlyLimit":100} as unknown as BudgetCheckInput);
    expect(result).toEqual({"dailySpend":0,"monthlySpend":0,"dailyUtilization":0,"monthlyUtilization":0,"alerts":[],"status":"ok"});
  });
});
