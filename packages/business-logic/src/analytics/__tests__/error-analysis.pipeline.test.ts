/**
 * @generated by @flusk/forge — pipeline generator (test)
 */
import { describe, it, expect } from 'vitest';
import { errorAnalysis } from '../error-analysis.pipeline.js';
import type { ErrorAnalysisInput } from '../error-analysis.pipeline.js';

describe('errorAnalysis pipeline', () => {
  it('no errors', () => {
    const result = errorAnalysis({"calls":[]} as unknown as ErrorAnalysisInput);
    expect(result).toEqual({"totalErrors":0,"errorRate":0,"wastedCost":0,"byModel":{},"topErrors":[]});
  });
});
