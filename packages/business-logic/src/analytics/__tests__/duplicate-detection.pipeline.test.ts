/**
 * @generated by @flusk/forge — pipeline generator (test)
 */
import { describe, it, expect } from 'vitest';
import { duplicateDetection } from '../duplicate-detection.pipeline.js';
import type { DuplicateDetectionInput } from '../duplicate-detection.pipeline.js';

describe('duplicateDetection pipeline', () => {
  it('no duplicates', () => {
    const result = duplicateDetection({"calls":[]} as unknown as DuplicateDetectionInput);
    expect(result).toEqual({"exactDuplicates":0,"nearDuplicates":0,"wastedCost":0,"groups":[]});
  });
});
