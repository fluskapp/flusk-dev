/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/text/truncate.fn.yaml
 */

/**
 * Truncate a string to a maximum length, appending an ellipsis if cut
 */
export function truncate(text: string, maxLength: number, ellipsis: string): string {
  return text.length <= maxLength
  ? text
  : text.slice(0, maxLength - (ellipsis ?? '...').length) + (ellipsis ?? '...');
}
