/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { truncate } from '../truncate.function.js';

describe('truncate', () => {
  it('no truncation needed', () => {
    expect(truncate("hello", 10, "...")).toEqual("hello");
  });

  it('truncates with ellipsis', () => {
    expect(truncate("hello world", 8, "...")).toEqual("hello...");
  });

  it('exact length', () => {
    expect(truncate("hello", 5, "...")).toEqual("hello");
  });
});
