/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/training/extract-trigrams.fn.yaml
 */

/**
 * Extract word trigrams from text for similarity comparison
 */
export function extractTrigrams(text: string): string[] {
  return (() => {
  const words = text.toLowerCase().replace(/[^\w\s]/g, '').split(/\s+/).filter(Boolean);
  if (words.length < 3) return words.length > 0 ? [words.join(' ')] : [];
  const trigrams: string[] = [];
  for (let i = 0; i <= words.length - 3; i++) {
    trigrams.push(words.slice(i, i + 3).join(' '));
  }
  return trigrams;
})();
}
