/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { shouldCollect } from '../should-collect.function.js';

describe('shouldCollect', () => {
  it('collects from teacher model', () => {
    expect(shouldCollect("anthropic", "claude-sonnet-4-20250514", "Explain quantum computing", "Quantum computing uses qubits...", "ok", ["claude-sonnet-4-20250514","gpt-4o"])).toEqual(true);
  });

  it('skips error calls', () => {
    expect(shouldCollect("openai", "gpt-4o", "Hello world", "Error occurred", "error", ["gpt-4o"])).toEqual(false);
  });

  it('skips non-teacher models', () => {
    expect(shouldCollect("openai", "gpt-3.5-turbo", "Hello world test", "Hi there response", "ok", ["gpt-4o","claude-sonnet-4-20250514"])).toEqual(false);
  });

  it('skips short prompts', () => {
    expect(shouldCollect("openai", "gpt-4o", "Hi", "Hello! How can I help?", "ok", ["gpt-4o"])).toEqual(false);
  });
});
