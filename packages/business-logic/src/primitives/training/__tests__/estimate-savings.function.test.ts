/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { estimateSavings } from '../estimate-savings.function.js';

describe('estimateSavings', () => {
  it('calculates savings correctly', () => {
    expect(estimateSavings(1000, 500, 200, 0.015, 0.06, 0.001, 0.002)).toEqual({"teacherCost":19.5,"slmCost":0.9,"savings":18.6,"savingsPercent":95.38461538461539});
  });
});
