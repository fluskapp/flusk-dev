/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/training/format-as-jsonl.fn.yaml
 */

/**
 * Format a training pair as JSONL chat format for fine-tuning
 */
export function formatAsJsonl(prompt: string, completion: string, systemPrompt: string): string {
  return (() => {
  const messages: Array<{ role: string; content: string }> = [];
  if (systemPrompt) messages.push({ role: 'system', content: systemPrompt });
  messages.push({ role: 'user', content: prompt });
  messages.push({ role: 'assistant', content: completion });
  return JSON.stringify({ messages });
})();
}
