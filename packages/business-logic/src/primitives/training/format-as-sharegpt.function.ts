/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/training/format-as-sharegpt.fn.yaml
 */

/**
 * Format a training pair as ShareGPT format for fine-tuning
 */
export function formatAsSharegpt(prompt: string, completion: string, systemPrompt: string): string {
  return (() => {
  const conversations: Array<{ from: string; value: string }> = [];
  if (systemPrompt) conversations.push({ from: 'system', value: systemPrompt });
  conversations.push({ from: 'human', value: prompt });
  conversations.push({ from: 'gpt', value: completion });
  return JSON.stringify({ conversations });
})();
}
