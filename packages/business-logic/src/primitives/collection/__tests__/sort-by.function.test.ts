/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { sortBy } from '../sort-by.function.js';

describe('sortBy', () => {
  it('sort ascending', () => {
    expect(sortBy([{"v":3},{"v":1},{"v":2}], "v", "asc")).toEqual([{"v":1},{"v":2},{"v":3}]);
  });

  it('sort descending', () => {
    expect(sortBy([{"v":1},{"v":3},{"v":2}], "v", "desc")).toEqual([{"v":3},{"v":2},{"v":1}]);
  });
});
