/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/collection/sort-by.fn.yaml
 */

/**
 * Sort an array of objects by a key, ascending or descending
 */
export function sortBy<T>(items: T[], key: string, direction: "asc" | "desc"): T[] {
  return [...items].sort((a, b) => {
  const av = (a as Record<string, unknown>)[key] as string | number;
  const bv = (b as Record<string, unknown>)[key] as string | number;
  const cmp = av < bv ? -1 : av > bv ? 1 : 0;
  return direction === 'desc' ? -cmp : cmp;
});
}
