/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/collection/unique-by.fn.yaml
 */

/**
 * Return array with duplicates removed, deduplicating by a key
 */
export function uniqueBy<T>(items: T[], key: string): T[] {
  return items.filter((item, index, arr) => {
  const k = (item as Record<string, unknown>)[key];
  return arr.findIndex(i => (i as Record<string, unknown>)[key] === k) === index;
});
}
