/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { formatDuration } from '../format-duration.function.js';

describe('formatDuration', () => {
  it('milliseconds', () => {
    expect(formatDuration(300)).toEqual("300ms");
  });

  it('seconds', () => {
    expect(formatDuration(1500)).toEqual("1.5s");
  });

  it('minutes and seconds', () => {
    expect(formatDuration(150000)).toEqual("2m 30s");
  });

  it('exact minutes', () => {
    expect(formatDuration(120000)).toEqual("2m");
  });
});
