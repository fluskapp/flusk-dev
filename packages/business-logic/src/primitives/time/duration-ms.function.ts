/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/time/duration-ms.fn.yaml
 */

/**
 * Calculate duration in milliseconds between two ISO timestamps
 */
export function durationMs(startedAt: string, endedAt: string): number {
  return Math.max(0, new Date(endedAt).getTime() - new Date(startedAt).getTime());
}
