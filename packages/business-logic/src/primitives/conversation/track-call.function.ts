/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/conversation/track-call.fn.yaml
 */

export interface TrackCallResult {
  turnCount: number;
  totalInputTokens: number;
  totalOutputTokens: number;
  totalCost: number;
}

/**
 * Build updated conversation totals from a new LLM call
 */
export function trackCall(
  existingTurnCount: number,
  existingInputTokens: number,
  existingOutputTokens: number,
  existingCost: number,
  newInputTokens: number,
  newOutputTokens: number,
  newCost: number,
): TrackCallResult {
  return {
    turnCount: existingTurnCount + 1,
    totalInputTokens: existingInputTokens + newInputTokens,
    totalOutputTokens: existingOutputTokens + newOutputTokens,
    totalCost: existingCost + newCost,
  };
}
