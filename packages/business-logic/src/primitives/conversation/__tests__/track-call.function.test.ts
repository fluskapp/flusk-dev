/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { trackCall } from '../track-call.function.js';

describe('trackCall', () => {
  it('first call', () => {
    expect(trackCall(0, 0, 0, 0, 100, 50, 0.005)).toEqual({"turnCount":1,"totalInputTokens":100,"totalOutputTokens":50,"totalCost":0.005});
  });

  it('subsequent call', () => {
    const result = trackCall(3, 500, 200, 0.05, 150, 80, 0.01);
    expect(result.turnCount).toBe(4);
    expect(result.totalInputTokens).toBe(650);
    expect(result.totalOutputTokens).toBe(280);
    expect(result.totalCost).toBeCloseTo(0.06);
  });
});
