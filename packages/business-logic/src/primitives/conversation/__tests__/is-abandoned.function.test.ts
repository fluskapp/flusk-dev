/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { isAbandoned } from '../is-abandoned.function.js';

describe('isAbandoned', () => {
  it('not abandoned', () => {
    expect(isAbandoned(1000000, 1060000, 30)).toEqual(false);
  });

  it('abandoned', () => {
    expect(isAbandoned(1000000, 5000000, 30)).toEqual(true);
  });

  it('exactly at threshold', () => {
    expect(isAbandoned(0, 1800000, 30)).toEqual(false);
  });
});
