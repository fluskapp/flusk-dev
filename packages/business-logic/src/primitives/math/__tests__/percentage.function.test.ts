/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { percentage } from '../percentage.function.js';

describe('percentage', () => {
  it('basic percentage', () => {
    expect(percentage(25, 100)).toEqual(25);
  });

  it('zero total', () => {
    expect(percentage(5, 0)).toEqual(0);
  });

  it('full percentage', () => {
    expect(percentage(50, 50)).toEqual(100);
  });
});
