/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/cost/aggregate-cost.fn.yaml
 */

/**
 * Aggregate total cost from an array of individual call costs
 */
export function aggregateCost(costs: number[]): { total: number; count: number; average: number; min: number; max: number } {
  return costs.length === 0
  ? { total: 0, count: 0, average: 0, min: 0, max: 0 }
  : {
      total: costs.reduce((a, b) => a + b, 0),
      count: costs.length,
      average: costs.reduce((a, b) => a + b, 0) / costs.length,
      min: Math.min(...costs),
      max: Math.max(...costs),
    };
}
