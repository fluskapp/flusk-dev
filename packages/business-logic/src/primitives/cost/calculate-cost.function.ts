/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/cost/calculate-cost.fn.yaml
 */

/**
 * Calculate LLM call cost from token counts and per-token pricing
 */
export function calculateCost(inputTokens: number, outputTokens: number, pricePerInputToken: number, pricePerOutputToken: number): number {
  return (inputTokens * pricePerInputToken) + (outputTokens * pricePerOutputToken);
}
