/**
 * @generated by @flusk/cli — function generator
 * Source: packages/schema/functions/cost/compare-cost.fn.yaml
 */

/**
 * Compare two costs and return the difference and savings percentage
 */
export function compareCost(baseline: number, optimized: number): { savings: number; savingsPercent: number; cheaper: boolean } {
  return {
  savings: baseline - optimized,
  savingsPercent: baseline === 0 ? 0 : ((baseline - optimized) / baseline) * 100,
  cheaper: optimized < baseline,
};
}
