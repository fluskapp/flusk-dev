/**
 * @generated by @flusk/cli — function generator
 */
import { describe, it, expect } from 'vitest';
import { compareCost } from '../compare-cost.function.js';

describe('compareCost', () => {
  it('optimized is cheaper', () => {
    expect(compareCost(1, 0.25)).toEqual({"savings":0.75,"savingsPercent":75,"cheaper":true});
  });

  it('same cost', () => {
    expect(compareCost(1, 1)).toEqual({"savings":0,"savingsPercent":0,"cheaper":false});
  });

  it('zero baseline', () => {
    expect(compareCost(0, 0)).toEqual({"savings":0,"savingsPercent":0,"cheaper":false});
  });
});
