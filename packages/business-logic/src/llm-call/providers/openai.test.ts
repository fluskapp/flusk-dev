/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { OPENAI_PRICING } from './openai.pricing.js';
import { OPENAI_SYSTEM_VALUES, OPENAI_MODEL_PREFIXES } from './openai.span-config.js';

describe('openai provider', () => {
  it('has pricing for all known models', () => {
    const expected = [
    'gpt-4o',
    'gpt-4o-mini',
    'gpt-4-turbo',
    'gpt-4.1',
    'gpt-4.1-mini',
    'gpt-4.1-nano',
    'o1',
    'o1-mini',
    'o1-pro',
    'o3',
    'o3-mini',
    'o4-mini',
    ];
    for (const m of expected) {
      expect(OPENAI_PRICING[m]).toBeDefined();
    }
  });

  it('has system values', () => {
    expect(OPENAI_SYSTEM_VALUES.length).toBeGreaterThan(0);
  });

  it('has model prefixes', () => {
    expect(OPENAI_MODEL_PREFIXES.length).toBeGreaterThan(0);
  });
});
