/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { COHERE_PRICING } from './cohere.pricing.js';
import { COHERE_SYSTEM_VALUES, COHERE_MODEL_PREFIXES } from './cohere.span-config.js';

describe('cohere provider', () => {
  it('has pricing for all known models', () => {
    const expected = [
    'command-r-plus',
    'command-r',
    'command',
    'command-light',
    'embed-english-v3.0',
    'embed-multilingual-v3.0',
    'rerank-english-v3.0',
    ];
    for (const m of expected) {
      expect(COHERE_PRICING[m]).toBeDefined();
    }
  });

  it('has system values', () => {
    expect(COHERE_SYSTEM_VALUES.length).toBeGreaterThan(0);
  });

  it('has model prefixes', () => {
    expect(COHERE_MODEL_PREFIXES.length).toBeGreaterThan(0);
  });
});
