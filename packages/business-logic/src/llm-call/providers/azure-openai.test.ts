/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { AZURE_OPENAI_PRICING } from './azure-openai.pricing.js';
import { AZURE_OPENAI_SYSTEM_VALUES, AZURE_OPENAI_MODEL_PREFIXES } from './azure-openai.span-config.js';

describe('azure-openai provider', () => {
  it('has pricing for all known models', () => {
    const expected = [
    'gpt-4o',
    'gpt-4o-mini',
    'gpt-4-turbo',
    'gpt-35-turbo',
    'o1',
    'o3',
    'o3-mini',
    'o4-mini',
    ];
    for (const m of expected) {
      expect(AZURE_OPENAI_PRICING[m]).toBeDefined();
    }
  });

  it('has system values', () => {
    expect(AZURE_OPENAI_SYSTEM_VALUES.length).toBeGreaterThan(0);
  });

  it('has model prefixes', () => {
    expect(AZURE_OPENAI_MODEL_PREFIXES.length).toBeGreaterThan(0);
  });
});
