/**
 * @generated by @flusk/cli — provider generator
 */
import { describe, it, expect } from 'vitest';
import { GOOGLE_PRICING } from './google.pricing.js';
import { GOOGLE_SYSTEM_VALUES, GOOGLE_MODEL_PREFIXES } from './google.span-config.js';

describe('google provider', () => {
  it('has pricing for all known models', () => {
    const expected = [
    'gemini-2.5-pro',
    'gemini-2.5-flash',
    'gemini-2.0-flash',
    'gemini-1.5-pro',
    'gemini-1.5-flash',
    ];
    for (const m of expected) {
      expect(GOOGLE_PRICING[m]).toBeDefined();
    }
  });

  it('has system values', () => {
    expect(GOOGLE_SYSTEM_VALUES.length).toBeGreaterThan(0);
  });

  it('has model prefixes', () => {
    expect(GOOGLE_MODEL_PREFIXES.length).toBeGreaterThan(0);
  });
});
