// --- BEGIN GENERATED ---
/**
 * @generated by @flusk/forge — action generator
 */
import { describe, it, expect } from 'vitest';
import type { AnalyzeInputs, AnalyzeOutputs } from '../src/types.js';

describe('analyze action types', () => {
  it('should define input type', () => {
    const inputs: AnalyzeInputs = {
      script: 'test',
      provider: 'test',
      budget: 'test',
      explain: 'test',
      explainProvider: 'test',
      token: 'test',
    };
    expect(inputs).toBeDefined();
  });

  it('should define output type', () => {
    const outputs: AnalyzeOutputs = {
      totalCost: 'test',
      totalCalls: 'test',
      insightsJson: 'test',
    };
    expect(outputs).toBeDefined();
  });
});
// --- END GENERATED ---
