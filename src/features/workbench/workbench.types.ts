/** Committable team UI defaults: <repo>/.flusk/workbench.json (H0 D6). */
export interface WorkbenchFile {
	/** Run configuration preselected in the toolbar runner widget. */
	defaultRunConfig?: string;
}

export const WORKBENCH_FILE = ".flusk/workbench.json";
