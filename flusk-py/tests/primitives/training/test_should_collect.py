# @generated by @flusk/cli — python function generator
import pytest
from flusk.primitives.training.should_collect import should_collect


def test_collects_from_teacher_model():
    assert should_collect("anthropic", "claude-sonnet-4-20250514", "Explain quantum computing", "Quantum computing uses qubits...", "ok", ["claude-sonnet-4-20250514","gpt-4o"]) == True


def test_skips_error_calls():
    assert should_collect("openai", "gpt-4o", "Hello world", "Error occurred", "error", ["gpt-4o"]) == False


def test_skips_nonteacher_models():
    assert should_collect("openai", "gpt-3.5-turbo", "Hello world test", "Hi there response", "ok", ["gpt-4o","claude-sonnet-4-20250514"]) == False


def test_skips_short_prompts():
    assert should_collect("openai", "gpt-4o", "Hi", "Hello! How can I help?", "ok", ["gpt-4o"]) == False
