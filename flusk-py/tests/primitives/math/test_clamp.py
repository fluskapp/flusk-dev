# @generated by @flusk/cli — python function generator
import pytest
from flusk.primitives.math.clamp import clamp


def test_below_min():
    assert clamp(-5, 0, 10) == 0


def test_above_max():
    assert clamp(15, 0, 10) == 10


def test_within_range():
    assert clamp(5, 0, 10) == 5
